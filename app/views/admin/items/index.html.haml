%h2 商品一覧

%table.table
  %thead.thead-dark
    %tr
      %th{ scope: 'col' } ID
      %th{ scope: 'col' } 商品名
      %th{ scope: 'col' } 価格
      %th{ scope: 'col' } 非表示フラグ
      %th{ scope: 'col' } 表示順
      %th
      %th
      %th

  %tbody
    - @items.each do |item|
      %tr
        %td= item.id
        %td= item.item_name
        %td= item.price
        %td= item.disabled
        %td= item.dspo
        %td= link_to '表示', admin_item_path(item), class: 'btn btn-outline-dark btn-sm'
        %td= link_to '編集', edit_admin_item_path(item), class: 'btn btn-outline-info btn-sm'
        %td= link_to '削除', admin_item_path(item), class: 'btn btn-danger btn-sm', method: :delete, data: { confirm: '削除してよろしいですか？' }
= paginate @items

= link_to '新しい商品', new_admin_item_path, class: 'btn btn-primary'
