<% unless @good.errors.any? %>
  $('.good_button_<%= @diary.id %>').html("<%=j render 'good_button', diary: @diary %>");
  toastr.info('Good! を解除しました。');
<% else %>
  toastr.warning('<%= @good.toastr_error_message("Good!を解除できませんでした：") %>');
<% end %>
