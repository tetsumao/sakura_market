= "#{@good.diary.user.user_name} 様"

= "あなたの日記(#{@good.diary.created_at.strftime('%Y/%m/%d %H:%M')})にGood!が付きました。"
\------------------------------------------------------------
= "#{@good.user.user_name} #{@good.created_at.strftime('%Y/%m/%d %H:%M')}"
\------------------------------------------------------------
