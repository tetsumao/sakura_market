= "#{@comment.diary.user.user_name} 様"

= "あなたの日記(#{@comment.diary.created_at.strftime('%Y/%m/%d %H:%M')})にコメントが登録されました。"
\------------------------------------------------------------
= "#{@comment.user.user_name} #{@comment.created_at.strftime('%Y/%m/%d %H:%M')}"
= @comment.content
\------------------------------------------------------------
