%h2= "注文履歴（注文日：#{@order.created_at.strftime('%Y/%m/%d')}）"

= render 'orders/order', order: @order, order_display_mode: :trader

= link_to '注文履歴へ戻る', trader_orders_path, class: 'btn btn-outline-secondary'
